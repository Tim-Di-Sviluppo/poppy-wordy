{
  "name": "poppy-wordy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "homepage": "https://Tim-Di-Sviluppo.github.io/poppy-wordy/",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.88.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.6.2",
    "vite": "^6.0.1"
  }
}
